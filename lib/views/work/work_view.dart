import 'package:flutter/material.dart';

class WorkView extends StatelessWidget {
  const WorkView({Key key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Work'),
      
    );
  }
}